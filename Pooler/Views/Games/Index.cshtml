@model Pooler.Domain.Entities.PoolGame

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div>
    @Html.ActionLink("New Game", "NewGame", "Games", new object { }, new { @class = "btn btn-success" })
</div>

<div>
    @Html.Label("Games Recorded: ")
    @Html.Label("0")
</div>